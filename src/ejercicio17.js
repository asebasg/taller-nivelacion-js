// Declara tres variables numéricas: a, b y c.
// Utiliza operadores de comparación y lógicos para determinar
// si al menos dos de las variables son iguales entre sí.

let a = 2;
let b = 4;
let c = 2;

if (a === b || a === c || b === c) {
  console.log("Al menos dos de las variables cumplen una igualdad entre si");
} else {
  console.log(
    "Ninguna de las variables existentes cumple con alguna igualdad entre si"
  );
}
