/**
 * Declara una variable base y otra exponente,
 * y calcula la potencia utilizando el operador de exponenciación (*).
 */

let base = 5;
let exponente = 3;

let resultado = base ** exponente;

console.log(`${base} ^ ${exponente}`);
console.log(resultado);